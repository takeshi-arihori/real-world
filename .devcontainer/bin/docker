#!/bin/sh
set -eu

DOCKER_BIN=/usr/bin/docker

if [ "$(id -u)" -eq 0 ]; then
  exec "$DOCKER_BIN" "$@"
fi

if command -v sudo >/dev/null 2>&1; then
  exec sudo "$DOCKER_BIN" "$@"
fi

exec "$DOCKER_BIN" "$@"
